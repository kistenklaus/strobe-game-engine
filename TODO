

Remove ImageLayouts
  - transition to present is still required but we can try to hide it completely, if we know that the image will be in a general layout
    => still requires us to figure out some synchronization but the idea sounds good.
  - refactor image / buffer barriers => global barriers may just be more efficient!

Staging Allocations: CommandBuffer / CommandPool


MemoryPool <- internal host synchronization

CommandBuffer::pipeline_barrier
DebugUtils set_name 
bind_vertex_buffer
bind_index_buffer
copy_buffer




CommandBuffer
  - Staging arena (internal VmaPool, may be managed by cmdpool?)

ShaderPrograms 
  - linking shader objects


Milestones:
VertexBuffer:
  - command buffer copy / staging commands
  - How do we want to handle staging! (command buffer arena ???)
    => The dream would be cmd.alloc_tmp() -> DeviceAddress or something like that.
    => Maybe a cmd_upload would be nice to upload a buffer to a buffer even if it's device local!
IndexBuffer:


DescriptorHeap: 
  - really really big one we have to think about a lot!!
  - expect many refactors!!! We will not get it right the first time.


