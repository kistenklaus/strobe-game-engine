#ifndef STROBE_RENDERER_BACKEND_TRIGON_SUBMISSION_H
#define STROBE_RENDERER_BACKEND_TRIGON_SUBMISSION_H

#include "mat.h"

int trigon_draw(unsigned int frame, unsigned int mesh, unsigned int material, mat4f* transform);

#endif
